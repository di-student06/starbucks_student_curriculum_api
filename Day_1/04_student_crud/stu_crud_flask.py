# create a get and put route using the flask framework
from flask import Flask, request

app = Flask(__name__)


# create a get route

# create a put route

# create a route that can be a get or put route and uses a url parameter

if __name__ == "__main__":
    app.run(debug=True)